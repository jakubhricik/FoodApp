import React from "react";
import {
  View,
  Text,
  TouchableOpacity,
  ImageBackground,
  StatusBar,
  StyleSheet,
} from "react-native";

const Login = ({ navigation }) => {
  return (
    <View>
      <Text></Text>
    </View>
  );
};

export default Login;
